@model IEnumerable<CAA_TestApp.Models.Organize>

@{
    ViewData["Title"] = "Index";
}

<h1>Product Organization</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.OrganizedBy)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.OrganizedBy)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ID">Edit</a> 
                    <a asp-action="Details" asp-route-id="@item.ID" hidden>Details</a>
                <a asp-action="Delete" asp-route-id="@item.ID" hidden>Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
